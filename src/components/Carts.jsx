import React from 'react';

const Carts = ({carts    , setCarts}) => {
    
    return (
        <div>
            Length of  cart   is :  {carts.length}
        </div>
    );
};

export default Carts;